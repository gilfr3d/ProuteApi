BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Routes] ALTER COLUMN [distance] FLOAT(53) NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
