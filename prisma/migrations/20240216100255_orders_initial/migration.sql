BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Orders] ALTER COLUMN [city] NVARCHAR(1000) NULL;
ALTER TABLE [dbo].[Orders] ALTER COLUMN [shipping_address] NVARCHAR(1000) NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
